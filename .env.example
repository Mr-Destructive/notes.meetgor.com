# Database
# For Turso: libsql://[database]-[org].turso.io?authToken=[token]
# For Local SQLite: file:./blog.db
DATABASE_URL=file:./blog.db

# Authentication (choose ONE):
# Option 1: Use raw password (simplest for dev)
ADMIN_PASSWORD=changeme

# Option 2: Use pre-hashed password (more secure for production)
# PASSWORD_HASH=<bcrypt-hash-here>
# To generate: go run -e "package main; import \"golang.org/x/crypto/bcrypt\"; func main() { h, _ := bcrypt.GenerateFromPassword([]byte(\"password\"), 10); println(string(h)) }"

# JWT secret for session tokens
JWT_SECRET=your-secret-key-change-this

# Server
PORT=8080
HOST=localhost

# Environment
ENV=development
